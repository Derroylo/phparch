# PHP Architecture Validator

A PHPUnit-inspired tool for validating project architecture. Define architecture tests in a dedicated folder, and use selectors and assertions to validate your codebase structure.

WIP: Working prototype but still in early development phase
